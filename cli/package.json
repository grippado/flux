{
  "name": "flux-cli",
  "version": "1.27.0",
  "private": true,
  "scripts": {
    "test": "bun test",
    "build": "bun build --compile src/index.ts --outfile flux",
    "setup": "bash scripts/setup.sh"
  }
}
